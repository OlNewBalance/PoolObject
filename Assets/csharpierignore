# .csharpierignore (репозиторный корень)

# 1) Явно игнорировать XML/props/targets, чтобы CSharpier не трогал их
**/*.props
**/*.targets
**/*.xml

# 2) Игнорировать все C# файлы по умолчанию
**/*.cs

# 3) Разрешить (unignore) директории Logic и C# файлы внутри них
!**/Logic/
!**/Logic/**/*.cs
